(function(){
  try {
    // Seed storage with last synced data (updated by GitHub Action)
    // This file will be auto-updated. Leaving empty seed.
    var seed = localStorage.getItem('ps:entries:v1');
    // no-op if already present
  } catch (e) {}
})();

